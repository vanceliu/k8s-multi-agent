"""Prompt templates for context compaction."""

MEMORY_FLUSH_PROMPT = """你是一個記憶提取助手。以下是一段即將被壓縮的對話歷史。
請從中提取值得長期記住的重要資訊，分類如下：

1. **fact**：使用者提到的個人資訊、偏好、專案背景、技術決策
2. **decision**：討論後做出的技術或業務決策及其原因
3. **todo**：提到但尚未完成的事項（含截止日期，如有）
4. **lesson**：從錯誤或討論中學到的經驗教訓

對話歷史：
{messages_text}

請以 JSON 格式回傳，每個項目包含 category 和 content 欄位：
[{{"category": "fact", "content": "..."}}, ...]

規則：
- 只提取確實重要、值得跨 session 記住的資訊
- 不要提取一般性的問答內容或已完成的操作步驟
- content 用一句話精簡描述，包含必要的上下文
- 如果沒有值得記住的資訊，回傳空陣列 []"""

SUMMARIZE_PROMPT = """你是一個對話摘要助手。請將以下對話歷史壓縮為簡潔的摘要。

要求：
1. 保留關鍵上下文（討論了什麼主題、做了什麼決定、目前進度）
2. 保留重要的技術細節（檔案路徑、指令、錯誤訊息、程式碼片段名稱）
3. 用條列式整理，每點一句話
4. 摘要長度控制在原文的 10%~20%
5. 使用與原文相同的語言（中文對話用中文摘要）

對話歷史：
{messages_text}

請直接輸出摘要內容，不需要額外標題或格式包裝。"""
