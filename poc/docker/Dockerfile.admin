FROM python:3.13-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx pyyaml

COPY poc/ /app/poc/

EXPOSE 8090

CMD ["uvicorn", "poc.admin.main:app", "--host", "0.0.0.0", "--port", "8090"]
