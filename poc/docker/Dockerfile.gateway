FROM python:3.13-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx python-multipart

COPY poc/ /app/poc/

EXPOSE 8000

CMD ["uvicorn", "poc.gateway.main:app", "--host", "0.0.0.0", "--port", "8000"]
